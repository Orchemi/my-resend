version: "3.8"

services:
  my-resend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env.local
    restart: unless-stopped

  # Optional: PostgreSQL database (uncomment to run alongside)
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: my_resend
  #     POSTGRES_USER: my_resend
  #     POSTGRES_PASSWORD: your-password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #     - ./database.sql:/docker-entrypoint-initdb.d/init.sql
  #   ports:
  #     - "5432:5432"
# volumes:
#   postgres_data:
